<p>座席状況</p>
<%= @display_date.strftime("%Y年%m月%d日%-H時%m分")%>時点
<% if @open_time == false %>
<p>現在は営業時間外です。表示は翌営業日10:00の座席状況です。</p>
<% end %>
<table>
  <thead>
    <% @seats.each do |seat| %>
      <th>
        <%= link_to reservations_path(seat_id: seat.id) do %>
          <%= seat.name %>
        <% end %>
      </th>
    <% end %>
  </thead>
  <tbody>
    <tr>
    <% @seats.each do |seat| %>
      <% if @reservations.display_reservation(seat.id, @reservation_time.id, @display_date).nil? %>
        <td>×</td>
      <% else %>
        <td>⚪︎</td>
      <% end %>
    <% end %>
    </tr>
  </tbody>
</table>

<%= image_tag 'seat.jpeg', style: "width:500px;" %>