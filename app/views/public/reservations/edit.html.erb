<div class="row my-3 mx-auto">
  <div class="col-md-8 offset-md-2 col-lg-6 offset-lg-3 h5">
    <div class="h2">座席予約変更</div>
    <p>利用可能人数を超える場合はキャンセル後に座席を変更して予約してください。</p>
    <%= form_with model: @reservation, url: reservation_path, method: :patch do |f| %>
      <div class="mt-3">日時<div>
      <div class="border border-secondary rounded bg-light p-2 mt-1">
        <%= l @reservation.start_time, format: :long %>
        <%= @reservation.reservation_time.start_at + '〜' + @reservation.reservation_time.end_at %>
      </div>
      <div class="mt-3">座席</div>
      <div class="border border-secondary rounded bg-light p-2 mt-1"><%= @reservation.seat.name %><%= @reservation.seat.limit_number %>名まで利用可</div>
      <div class="mt-3">予約者</div>
      <div class="border border-secondary rounded bg-light p-2 mt-1"><%= current_customer.last_name_kana %></div>
      <div class="form-group mt-3">
        <%= f.label "予約人数" %>
        <%= f.select :number_of_people, *[1..@reservation.seat.limit_number],{}, { class: "form-control border-secondary" } %>
      </div>
      <div class="form-group mt-3">
        <%= f.label "コメント" %>
        <%= f.text_area :comment, class: "form-control border-secondary", rows: "5" %>
      </div>
      <div class="form-group mt-3">
        <%= f.submit '変更する', class: "btn btn-outline-primary btn-block" %>
      </div>
    <% end %>
  </div>
</div>